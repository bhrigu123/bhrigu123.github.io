<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">

  <title><![CDATA[Category: Mac | Bhrigu Srivastava]]></title>
  <link href="https://bhrigu.me/blog/categories/mac/atom.xml" rel="self"/>
  <link href="https://bhrigu.me/"/>
  <updated>2020-07-02T01:04:50+05:30</updated>
  <id>https://bhrigu.me/</id>
  <author>
    <name><![CDATA[Bhrigu Srivastava]]></name>
    
  </author>
  <generator uri="http://octopress.org/">Octopress</generator>

  
  <entry>
    <title type="html"><![CDATA[Mac OS X - Command Line Tools to Edit Images Video and Audio]]></title>
    <link href="https://bhrigu.me/blog/2020/04/12/mac-os-x-command-line-tools-to-edit-media/"/>
    <updated>2020-04-12T21:24:41+05:30</updated>
    <id>https://bhrigu.me/blog/2020/04/12/mac-os-x-command-line-tools-to-edit-media</id>
    <content type="html"><![CDATA[<p>Sometimes it is quite convenient and quick to edit media files (for eg. resize images) via command line. Below are such helpful tools for Mac OS X.</p>

<!-- more -->


<hr />

<blockquote><h2>Table of contents</h2>

<ol>
<li><a href="#images">Images</a></li>
<li><a href="#videos">Videos</a></li>
<li><a href="#audio">Audio</a></li>
</ol>
</blockquote>

<hr />

<center>
    <img style="box-shadow: none;" src="https://bhrigu.me/images/mac.png">
</center>


<h2>Images <a name="images"></a></h2>

<p>Mac has an inbuild CLI tool called <em>sips</em>. It modifies the images in-place. To save the updated image in a separate copy, use <code>--out [outfilename]</code> in the command.</p>

<h4>Re-size image</h4>

<p><code>sips -z [height] [width] file.png</code>: Re-sizes image to specified dimensions, which may alter the actual aspect ratio.</p>

<p><code>sips -Z [heightwidth] file.png</code>: This maintains the aspect ratio and [heightwidht] value will be the maximum value that can be for the height and width of the image (the other dimension will get set acc to its aspect ratio).</p>

<p>Example</p>

<pre><code>sips -z 640 480 file1.png
sips -Z 640 file2.png
</code></pre>

<p>You can view the current dimension details of an image by looking at the image&rsquo;s info.</p>

<h4>Rotate image</h4>

<p><code>sips -r [degrees] file.png</code></p>

<p>Example:</p>

<pre><code>sips -r 90 file.jpg --out rotatedfile.jpg
</code></pre>

<p>Other options:<br/>
<code>-f [horizontal or vertical]</code>: Flips the image<br/>
<code>-c [pixelsH pixelsV]</code>: Crops the image to height and width<br/>
Check out all the options available by doing <code>sips --help</code></p>

<hr />

<h2>Videos <a name="videos"></a></h2>

<p>For video editing via command line, you&rsquo;ll have to install a tool called <code>ffmpeg</code> which will make your life easier.</p>

<p>You can install using Homebrew:</p>

<pre><code>$ brew install ffmpeg
</code></pre>

<h4>Change format</h4>

<p>Eg. convert mp4 to mov</p>

<pre><code>ffmpeg -i input.mp4 output.mov
</code></pre>

<p><em>Tip: You can also make gifs from video using this command</em></p>

<h4>Change resolution / dimension</h4>

<p>Eg. Change video resolution to 1280x720. Can be used to reduce the resolution and therefore the size</p>

<pre><code>ffmpeg -i input.mp4 -c:a copy -s 1280x720 output.mp4
</code></pre>

<h4>Trim video</h4>

<p>Eg. 1) Trim from 00:00:10 to 00:00:20 (hh:mm:ss)</p>

<pre><code>ffmpeg -i input.mp4 -ss 00:00:10 -to 00:00:20 -c copy output.mp4
</code></pre>

<p>Eg. 2) Trim ten seconds starting from 00:01:00</p>

<pre><code>ffmpeg -i input.mp4 -c:av copy -ss 00:01:00 -t 10 output.mp4
</code></pre>

<h4>Extract audio from video</h4>

<pre><code>ffmpeg -i clip.mp4 -vn audio_only.mp3
</code></pre>

<h2>Audio <a name="audio"></a></h2>

<p>We are going to use the same command line tool <em>ffmpeg</em> to edit audio files too. If you don&rsquo;t have it, install it via Homebrew.</p>

<pre><code>$ brew install ffmpeg
</code></pre>

<p>The commands are same as the commands for editing the videos actually.</p>

<h4>Trim audio</h4>

<p>Eg. Trim from 00:00:20 to 00:00:40 (hh:mm:ss)</p>

<pre><code>ffmpeg -ss -i input.mp3 00:00:20 -to 00:00:40 -c copy output.mp3
</code></pre>

<h4>Change format</h4>

<p>Eg. convert mp3 to wav</p>

<pre><code>ffmpeg -i input.mp3 output.wav
</code></pre>

<p>That&rsquo;s all for now. Hope you find these useful and get comfortable and quick with editing media files via Command Line.</p>
]]></content>
  </entry>
  
</feed>
